
def check_content(obj1: str, obj2: str) -> float:
    """
    Checking if the content of two objects is roughly same using sentiment analysis.

    Args:
        obj1: First Object to Check.
        obj2: Second Object to Check.

    Returns: Similarity of two objects in percentage (as a float).

    """
    # TODO: Implement Sentiment Analysis
    return float(obj1 == obj2)
